# WORKLOG（近期工作记录）

> 用途：记录“最近发生了什么”。最新条目放最上面。  
> 约定：每次任务结束至少新增 1 条。

---

## 模板

### YYYY-MM-DD - 任务名
- 背景：一句话描述问题或目标。
- 关键改动：`path/to/fileA`、`path/to/fileB`
- 验证结果：通过 / 未通过（附简要说明）
- 未完成事项：无 / 列出下一步

---

## 2026-03-24 - 折叠侧边栏 Tooltip 样式统一
- 背景：折叠态提示文案已显示，但使用原生 `title`，样式与全站 `Tooltip` 不一致。
- 关键改动：`components/SourcesList.tsx`
- 验证结果：通过。折叠态提示改为项目 `Tooltip` 组件，风格与全站一致。
- 未完成事项：无。

## 2026-03-24 - 折叠侧边栏文案与选中态优化
- 背景：折叠态不应显示“当前信息源选中高亮”，并需要统一提示文案“点击展开信息源”。
- 关键改动：`components/SourcesList.tsx`
- 验证结果：通过。移除折叠态头像 ring 高亮；提示文案按要求生效。
- 未完成事项：无。

## 2026-03-24 - 作者头像丢失修复
- 背景：左侧信息源与新闻卡片作者头像出现缺失。
- 关键改动：`components/SourcesList.tsx`、`components/NewsCard.tsx`
- 验证结果：通过。确认服务端无法直接拉取 `pbs.twimg.com` 后，为头像图片启用 `unoptimized`，改为浏览器直连加载。
- 未完成事项：如未来网络策略变化，可评估恢复 Next Image 优化链路。

